---
title: Spring 注解开发
date: 2026-06-06 10:16:46
permalink: /study/spring-annotation
categories:
  - 学习
tags:
  - spring
---

# Spring 注解开发

## 定义 Bean

| 注解 | 说明 |
|------|------|
| `@Component` | 通用 Bean 定义 |
| `@Controller` | 表现层 |
| `@Service` | 业务层 |
| `@Repository` | 数据层 |

但是作用是一致的，都是定义 Bean 的，区别只是为了更清晰的分层。

```java
@Component("bookDao")
public class BookDaoImpl implements BookDao { }
```

![注解定义 Bean](https://img.zhaizz.top/file/blog-website/spring/1630028345074.png)

## 纯注解开发

```java
@Configuration
@ComponentScan("com.itheima")
public class SpringConfig { }
```

```java
ApplicationContext ctx = new AnnotationConfigApplicationContext(SpringConfig.class);
```

![纯注解开发](https://img.zhaizz.top/file/blog-website/spring/1630029254372.png)

## 作用范围与生命周期

```java
@Repository
@Scope("prototype")           // 非单例
public class BookDaoImpl implements BookDao {
    @PostConstruct             // 初始化
    public void init() { }
    @PreDestroy                // 销毁
    public void destroy() { }
}
```

## 依赖注入注解

```java
@Service
public class BookServiceImpl implements BookService {
    @Autowired                // 按类型注入
    @Qualifier("bookDao")     // 按名称注入（需配合 @Autowired）
    private BookDao bookDao;

    @Value("itheima")         // 简单类型注入
    private String name;
}
```

```java
@Configuration
@PropertySource("jdbc.properties")  // 加载配置文件
public class SpringConfig { }
```

```java
@Value("${jdbc.driver}")            // 读取配置文件
private String driver;
```

## Spring 整合 MyBatis

```java
// MybatisConfig
@Bean
public SqlSessionFactoryBean sqlSessionFactory(DataSource dataSource){
    SqlSessionFactoryBean ssfb = new SqlSessionFactoryBean();
    ssfb.setTypeAliasesPackage("com.itheima.domain");
    ssfb.setDataSource(dataSource);
    return ssfb;
}

@Bean
public MapperScannerConfigurer mapperScannerConfigurer(){
    MapperScannerConfigurer msc = new MapperScannerConfigurer();
    msc.setBasePackage("com.itheima.dao");
    return msc;
}
```

![Spring 整合 MyBatis](https://img.zhaizz.top/file/blog-website/spring/1630138835057(1).png)

## Spring 整合 Junit

```java
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(classes = {SpringConfig.class})
public class AccountServiceTest {
    @Autowired
    private AccountService accountService;

    @Test
    public void testFindById(){
        System.out.println(accountService.findById(1));
    }
}
```
