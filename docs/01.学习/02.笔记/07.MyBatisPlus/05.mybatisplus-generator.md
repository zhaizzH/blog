---
title: MyBatisPlus 代码生成器
date: 2026-06-08 10:20:46
permalink: /study/mybatisplus-generator
categories:
  - 学习
tags:
  - mybatisplus
---

# MyBatisPlus 代码生成器

## 代码生成器原理

观察之前写的代码，会发现有很多重复内容，比如 Dao 接口、模型类等。将红色部分（模块相关的内容）去掉后，剩下的就是**模板**，红色部分就是**参数**。只需要传入不同的参数，就可以根据模板创建出不同模块的代码。

![Dao 模板](https://img.zhaizz.top/file/blog-website/mybatisplus/1631254075651.png)

![模型类模板](https://img.zhaizz.top/file/blog-website/mybatisplus/1631254344180.png)

要想完成代码自动生成，需要以下内容：

- **模板**：MyBatisPlus 提供
- **数据库相关配置**：读取数据库获取表和字段信息
- **开发者自定义配置**：手工配置，比如 ID 生成策略

## 代码生成器实现

### 步骤 1：导入依赖

```xml
<!--代码生成器-->
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-generator</artifactId>
    <version>3.4.1</version>
</dependency>

<!--velocity模板引擎-->
<dependency>
    <groupId>org.apache.velocity</groupId>
    <artifactId>velocity-engine-core</artifactId>
    <version>2.3</version>
</dependency>
```

### 步骤 2：创建代码生成类

```java
public class CodeGenerator {
    public static void main(String[] args) {
        //1.获取代码生成器的对象
        AutoGenerator autoGenerator = new AutoGenerator();

        //设置数据库相关配置
        DataSourceConfig dataSource = new DataSourceConfig();
        dataSource.setDriverName("com.mysql.cj.jdbc.Driver");
        dataSource.setUrl("jdbc:mysql://localhost:3306/mybatisplus_db?serverTimezone=UTC");
        dataSource.setUsername("root");
        dataSource.setPassword("root");
        autoGenerator.setDataSource(dataSource);

        //设置全局配置
        GlobalConfig globalConfig = new GlobalConfig();
        globalConfig.setOutputDir(System.getProperty("user.dir")+"/src/main/java");
        globalConfig.setOpen(false);              //生成完毕后是否打开生成代码所在的目录
        globalConfig.setAuthor("作者名");          //设置作者
        globalConfig.setFileOverride(true);        //是否覆盖原始生成的文件
        globalConfig.setMapperName("%sDao");       //数据层接口名，%s为占位符，指代模块名称
        globalConfig.setIdType(IdType.ASSIGN_ID);  //Id生成策略
        autoGenerator.setGlobalConfig(globalConfig);

        //设置包名相关配置
        PackageConfig packageInfo = new PackageConfig();
        packageInfo.setParent("com.aaa");          //设置生成的包名
        packageInfo.setEntity("domain");           //实体类包名
        packageInfo.setMapper("dao");              //数据层包名
        autoGenerator.setPackageInfo(packageInfo);

        //策略设置
        StrategyConfig strategyConfig = new StrategyConfig();
        strategyConfig.setInclude("tbl_user");     //设置当前参与生成的表名
        strategyConfig.setTablePrefix("tbl_");     //设置数据库表的前缀名称
        strategyConfig.setRestControllerStyle(true);   //是否启用Rest风格
        strategyConfig.setVersionFieldName("version"); //乐观锁字段名
        strategyConfig.setLogicDeleteFieldName("deleted"); //逻辑删除字段名
        strategyConfig.setEntityLombokModel(true); //是否启用lombok
        autoGenerator.setStrategy(strategyConfig);

        //2.执行生成操作
        autoGenerator.execute();
    }
}
```

### 步骤 3：运行程序

运行成功后，会在当前项目中生成 `controller`、`service`、`mapper` 和 `entity` 代码。

![生成结果](https://img.zhaizz.top/file/blog-website/mybatisplus/1631255110375.png)

代码生成器的代码内容可以直接从官方文档中获取并修改：`https://baomidou.com/guides/new-code-generator/`

## Service 层 CRUD

MP 提供了 `IService` 接口和 `ServiceImpl` 实现类，封装了业务层的基础增删改查操作。

### 使用方式

```java
// 接口
public interface UserService extends IService<User>{

}

// 实现类
@Service
public class UserServiceImpl extends ServiceImpl<UserDao, User> implements UserService{

}
```

### 测试

```java
@SpringBootTest
class Mybatisplus04GeneratorApplicationTests {

    @Autowired
    private IUserService userService;

    @Test
    void testFindAll() {
        List<User> list = userService.list();
        System.out.println(list);
    }
}
```
