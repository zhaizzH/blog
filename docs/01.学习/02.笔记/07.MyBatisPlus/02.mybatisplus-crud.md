---
title: MyBatisPlus 标准数据层开发
date: 2026-06-08 10:17:46
permalink: /study/mybatisplus-crud
categories:
  - 学习
tags:
  - mybatisplus
---

# MyBatisPlus 标准数据层开发

## 标准 CRUD 使用

MP 为数据层提供了标准的 CRUD 方法：

![CRUD 方法](https://img.zhaizz.top/file/blog-website/mybatisplus/1631018877517.png)

### 新增

```java
int insert(T t)
```

- **T**：泛型，用来保存新增数据
- **int**：返回值，新增成功后返回 1，没有新增成功返回 0

```java
@SpringBootTest
class Mybatisplus01QuickstartApplicationTests {

    @Autowired
    private UserDao userDao;

    @Test
    void testSave() {
        User user = new User();
        user.setName("黑马程序员");
        user.setPassword("itheima");
        user.setAge(12);
        user.setTel("4006184000");
        userDao.insert(user);
    }
}
```

新增成功后，数据库表中会添加一条数据。主键 ID 默认使用雪花算法生成（后续章节会详细讲解主键生成策略）。

### 删除

```java
int deleteById(Serializable id)
```

- **Serializable**：参数类型。使用 Serializable 是因为 String 和 Number 都是 Serializable 的子类，能作为主键的数据类型都已经是 Serializable 的子类，类似用 Object 接收任何数据类型
- **int**：返回值，删除成功返回 1，未删除数据返回 0

```java
@Test
void testDelete() {
    userDao.deleteById(1401856123725713409L);
}
```

### 修改

```java
int updateById(T t)
```

- **T**：泛型，需要修改的数据内容，因为是根据 ID 进行修改，所以传入的对象中需要有 ID 属性值
- **int**：返回值，修改成功后返回 1，未修改数据返回 0

```java
@Test
void testUpdate() {
    User user = new User();
    user.setId(1L);
    user.setName("Tom888");
    user.setPassword("tom888");
    userDao.updateById(user);
}
```

**说明：** 修改的时候，只修改实体对象中有值的字段。

### 根据 ID 查询

```java
T selectById(Serializable id)
```

- **Serializable**：主键 ID 的值
- **T**：根据 ID 查询只会返回一条数据

```java
@Test
void testGetById() {
    User user = userDao.selectById(2L);
    System.out.println(user);
}
```

### 查询所有

```java
List<T> selectList(Wrapper<T> queryWrapper)
```

- **Wrapper**：用来构建条件查询的条件，目前没有可直接传为 Null
- **List\<T\>**：返回的数据是一个集合

```java
@Test
void testGetAll() {
    List<User> userList = userDao.selectList(null);
    System.out.println(userList);
}
```

## 分页功能

分页查询使用的方法：

```java
IPage<T> selectPage(IPage<T> page, Wrapper<T> queryWrapper)
```

- **IPage**：用来构建分页查询条件，也是返回值类型。IPage 是一个接口，实现类为 `Page`
- **Wrapper**：用来构建条件查询的条件

### 步骤 1：调用方法传入参数获取返回值

```java
@Test
void testSelectPage(){
    //1 创建IPage分页对象,设置分页参数,1为当前页码，3为每页显示的记录数
    IPage<User> page=new Page<>(1,3);
    //2 执行分页查询
    userDao.selectPage(page,null);
    //3 获取分页结果
    System.out.println("当前页码值："+page.getCurrent());
    System.out.println("每页显示数："+page.getSize());
    System.out.println("一共多少页："+page.getPages());
    System.out.println("一共多少条数据："+page.getTotal());
    System.out.println("数据："+page.getRecords());
}
```

### 步骤 2：设置分页拦截器

MP 已经提供好了分页拦截器，只需要配置成 Spring 管理的 Bean 对象即可：

```java
@Configuration
public class MybatisPlusConfig {

    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor(){
        //1 创建MybatisPlusInterceptor拦截器对象
        MybatisPlusInterceptor mpInterceptor=new MybatisPlusInterceptor();
        //2 添加分页拦截器
        mpInterceptor.addInnerInterceptor(new PaginationInnerInterceptor());
        return mpInterceptor;
    }
}
```

### 步骤 3：开启 SQL 日志（可选）

如果想查看 MP 执行的 SQL 语句，可以在 `application.yml` 中配置：

```yml
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #打印SQL日志到控制台
```

开启日志功能性能会受到影响，调试完后记得关闭。
